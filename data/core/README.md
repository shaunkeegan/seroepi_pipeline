# data/core/

Input data for the `core` analysis mode (full dataset, all studies included).

## Subdirectories

| Directory | Contents |
|-----------|----------|
| `site_counts/` | Per-locus site count TSVs — one file per antigen × subset × count-variant combination |
